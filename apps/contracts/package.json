{
  "name": "@auto/contracts",
  "private": true,
  "exports": {
    "./factory-definitions": {
      "types": "./factory-definitions.ts",
      "default": "./factory-definitions.ts"
    }
  },
  "scripts": {
    "build": "hardhat compile",
    "check-types": "tsc -p tsconfig.json --noEmit",
    "compile": "hardhat compile",
    "test": "hardhat test",
    "migrate": "hardhat run scripts/deploy-factory.ts --network localhost",
    "migrate:legacy": "hardhat run scripts/deploy-vault-legacy.ts --network baseSepolia",
    "migrate:testnet": "hardhat run scripts/deploy-factory.ts --network baseSepolia",
    "clean": "hardhat clean"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-chai-matchers": "^2.0.0",
    "@nomicfoundation/hardhat-ethers": "^3.0.0",
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "@nomicfoundation/hardhat-verify": "^2.0.0",
    "@typechain/ethers-v6": "^0.5.0",
    "@typechain/hardhat": "^9.0.0",
    "@types/chai": "^4.3.20",
    "chai": "^4.3.0",
    "ethers": "^6.0.0",
    "hardhat": "^2.22.0",
    "hardhat-gas-reporter": "^1.0.0",
    "solidity-coverage": "^0.8.0",
    "ts-node": "^10.9.2",
    "typechain": "^8.0.0",
    "typescript": "catalog:"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.3.0",
    "dotenv": "catalog:",
    "mocha": "^11.7.5"
  }
}
